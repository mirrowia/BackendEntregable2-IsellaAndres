const fs = requiere("fs").promises;
class Contenedor {
  constructor(fileName) {
    this.filename = fileName;
    this.id = id;
  }

  async save(Object) {
    fs.appendFileSync();
  }

  async getById(id) {}

  async getAll() {}
  deleteById(id) {}
  deleteAll() {}
}
